# Test Configuration
app.url=http://localhost:3000/
browser=chrome

# Timeouts (in seconds)
implicit.wait=10
explicit.wait=10
page.load.timeout=30

# Retry Configuration
retry.count=2

# Reporting
report.path=test-output/extent-report.html
screenshot.on.failure=true
